require.config({
    //baseUrl: '/',
    paths: {
        factory: 'factory',
        container: 'models/container',
        item: 'models/item',
        section: 'models/section'
    }
});

require(['htmlLoader'], function (htmlLoader) {

});